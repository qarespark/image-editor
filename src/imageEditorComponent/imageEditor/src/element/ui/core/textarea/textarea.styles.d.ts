import type { TextareaProps } from './textarea.props';
declare const Styled: {
    Textarea: import("styled-components").StyledComponent<"textarea", import("styled-components").DefaultTheme, {
        className: string;
        rows: 3;
    } & TextareaProps, "className" | "rows">;
};
export default Styled;
