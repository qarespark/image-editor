import type { RobotProps } from './robot.props';
declare const Styled: {
    Robot: import("styled-components").StyledComponent<"span", import("styled-components").DefaultTheme, {
        className: string;
    } & RobotProps, "className">;
};
export default Styled;
