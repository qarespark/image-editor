import { WithTheme } from '../../theme/entity';
export declare const sizeTooltipMixin: {
    sm: ({ theme: { typography: { font }, }, }: WithTheme) => any;
    md: ({ theme: { typography: { font }, }, }: WithTheme) => any;
};
